import { actionById, blockById, esc, fmtDateTime, statusLabel } from "./helpers.js";

function relationshipName(state, occurrence) { const relationship = (state.blocks || []).flatMap((block) => block.relationships || []).find((candidate) => candidate.id === occurrence.relationshipId); return relationship?.label || actionById(state, relationship?.refId)?.name || blockById(state, relationship?.refId)?.name || "Scheduled item"; }
function occurrenceCard(state, occurrence) { const relationship = (state.blocks || []).flatMap((block) => block.relationships || []).find((candidate) => candidate.id === occurrence.relationshipId); const canSkip = relationship?.config?.allowSkip === true; const canCompleteManually = relationship?.config?.manualCompletion === true; return `<article class="samt-card"><div class="samt-card-head"><div><h2>${esc(relationshipName(state, occurrence))}</h2><span class="samt-badge">${esc(statusLabel(occurrence.status))}</span></div><small>${esc(fmtDateTime(occurrence.scheduledAt || occurrence.availableFrom))}</small></div><p class="samt-muted">${esc(occurrence.snapshot?.schedule?.mode || "manual")} · ${occurrence.deadline ? `deadline ${esc(fmtDateTime(occurrence.deadline))}` : "no deadline"}</p><div class="samt-card-actions"><button class="samt-button primary" data-action="log-occurrence" data-id="${esc(occurrence.id)}">＋ Log</button>${!['completed','skipped','missed','expired'].includes(occurrence.status) ? `<button class="samt-button ghost" data-action="complete-occurrence" data-id="${esc(occurrence.id)}">Complete</button>${canSkip ? `<button class="samt-button ghost" data-action="skip-occurrence" data-id="${esc(occurrence.id)}" data-require-reason="${relationship?.config?.requireSkipReason === true ? "true" : "false"}">Skip</button>` : ""}` : ""}</div></article>`; }

export function renderActionListsView({ state = {} } = {}) {
  const lists = (state.blocks || []).filter((block) => block.type === "action_list"); const occurrences = (state.occurrences || []).filter((occurrence) => !["completed", "skipped", "missed", "expired"].includes(occurrence.status)); const tasks = [...(state.tasks || []), ...(state.quickTasks || [])].filter((task) => task.status !== "completed"); return `<section class="samt-page" aria-labelledby="todo-title"><header class="samt-page-head"><div><p class="samt-eyebrow">Occurrences · tasks · schedules</p><h1 id="todo-title">To-do</h1><p class="samt-muted">Action Lists generate occurrences; the list itself never pretends to be 100% complete.</p></div><div class="samt-card-actions"><button class="samt-button primary" data-action="new-quick-task">＋ Quick task</button><a class="samt-button ghost" href="#/blocks?type=action_list">Manage Action Lists</a></div></header><div class="samt-section-grid"><article class="samt-card"><div class="samt-card-head"><h2>Open occurrences</h2><span class="samt-count">${occurrences.length}</span></div><div class="samt-card-grid compact">${occurrences.length ? occurrences.map((occurrence) => occurrenceCard(state, occurrence)).join("") : `<p class="samt-empty">No open scheduled occurrences. Reconcile time or add a schedule.</p>`}</div></article><article class="samt-card"><div class="samt-card-head"><h2>Action Lists</h2><span class="samt-count">${lists.length}</span></div><div class="samt-list">${lists.length ? lists.map((block) => `<div class="samt-list-row"><div><a href="#/blocks/${encodeURIComponent(block.id)}"><strong>${esc(block.name)}</strong></a><small>${(block.relationships || []).length} relationships · ${esc(statusLabel(block.definitionStatus))}</small></div><a class="samt-button ghost small" href="#/blocks/${encodeURIComponent(block.id)}/edit">Edit</a></div>`).join("") : `<p class="samt-empty">No Action Lists yet.</p>`}</div></article></div><article class="samt-card"><div class="samt-card-head"><h2>Open tasks</h2><span class="samt-count">${tasks.length}</span></div><div class="samt-list">${tasks.length ? tasks.map((task) => `<div class="samt-list-row"><div><strong>${esc(task.name)}</strong><small>${esc(task.targetDate || task.date || "No date")}</small></div><button class="samt-button small" data-action="complete-task" data-kind="${task.id?.startsWith("quick") ? "quick" : "task"}" data-id="${esc(task.id)}">Done</button></div>`).join("") : `<p class="samt-empty">No open tasks.</p>`}</div></article></section>`;
}
